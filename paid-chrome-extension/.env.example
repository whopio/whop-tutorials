# Copy app-specific examples before running:
# cp apps/web/.env.example apps/web/.env.local
# cp extension/.env.example extension/.env

# Local dev only: opt in to mock mode (no Whop credentials needed).
# The web app refuses to boot if this is true while NODE_ENV=production.
WHOP_MOCK_MODE=true
